syntax = "proto3";
package zigbeealliance.distributedcomplianceledger.compliance;

option go_package = "github.com/zigbee-alliance/distributed-compliance-ledger/x/compliance/types";

message ComplianceInformation {
  
  string softwareVersionString = 1; 
  uint64 cDVersionNumber = 2; 
  uint64 softwareVersionCertificationStatus = 3; 
  repeated string history = 4; 
  int32 vid = 5; 
  int32 pid = 6; 
  uint64 softwareVersion = 7; 
  string certificationType = 8; 
}
